# Task #23 수행계획서 — rhwp Stable 감시와 전체 upstream 동기화 PR 자동화

GitHub Issue: [#23](https://github.com/postmelee/alhangeul-tauri/issues/23)
마일스톤: M010

## 목적

Alhangeul의 유일한 지속 upstream인 `edwardkim/rhwp`의 최신 Stable release를 자동으로
감시하고, 현재 pin과 차이가 있으면 Rust core와 전체 upstream Studio source를 동일한
release tag·resolved commit으로 갱신하는 reviewable draft PR 후보를 생성한다.

자동화는 release를 병합하거나 공개하지 않는다. tag provenance, 갱신 범위와 플랫폼 중립
검증을 기계적으로 제공하고, 별도 수용 task가 source diff와 Windows/Linux native gate를
판정할 수 있는 입력까지만 만든다.

## 배경

- 현재 pin은 `v0.8.2`, resolved commit
  `9b16aa9e23f476e2b335d7c029fc9f24a199d63c`이다.
- 2026-08-12 upstream `v0.8.4`는 draft/prerelease가 아닌 Stable release이며 annotated
  tag가 resolved commit `496333b27d21ddb9114ba9ae340bcb895870c9a7`을 가리킨다.
- `scripts/update-upstream.sh`는 승인된 tag·commit을 검증하고 source submodule, desktop
  Cargo lock, bundled WASM과 `rhwp-core.lock`을 원자적으로 갱신하지만 release 탐지와 PR
  생성을 소유하지 않는다.
- 전체 Studio source는 `third_party/rhwp/rhwp-studio`를 exact Vite root로 사용하므로
  submodule gitlink 갱신이 core와 Studio source를 함께 이동시킨다. 별도 local Studio
  복제본이나 prebuilt shadow bundle을 만들면 안 된다.
- Alhangeul-macOS에는 daily release check와 full sync PR workflow가 있으며 실제
  `v0.8.4` PR #463을 생성했다. 이를 설계 참고로만 사용하고 Tauri의 Windows/Linux,
  `pnpm`, submodule·WASM provenance 경계에 맞춰 구현한다.
- 현재 Tauri 저장소 Actions 기본 권한은 read이며 Actions의 PR 생성·승인 설정은 꺼져
  있다. 자동 PR용 repository variable·secret도 없다. macOS 저장소에만 기존 Alhangeul
  Automation GitHub App 설정이 있다.

## 범위

### 포함

- daily schedule과 `workflow_dispatch`를 지원하는 upstream Stable sync workflow
- 최신 release 또는 명시적 target tag의 draft/prerelease·정확한 `vX.Y.Z` 검증
- annotated/lightweight tag를 모두 고려한 resolved commit 결정과 current pin 비교
- no-drift, dry-run, new-drift, existing-open-PR, stale branch 상태의 명시적 분류
- `scripts/update-upstream.sh --tag ... --commit ... --run-checks`를 이용한 동일 release
  core·Studio source·WASM·Cargo lock·provenance 갱신
- README·UPSTREAM의 current pin과 exact pin 회귀 test처럼 기계 관리 가능한 참조만
  허용 목록과 예상 치환 횟수로 갱신
- `devel` 대상 `automation/rhwp-vX.Y.Z-full-sync` draft PR 생성과 중복 방지
- PR 본문의 old/new tag·commit, upstream URL, repository changed paths, 자동 검증 결과,
  native 미검증 상태와 후속 수용 task 안내
- 읽기 전용 resolve job과 GitHub App write job 분리, 최소 권한·secret 비노출 preflight
- helper·workflow 계약 test, `test:automation` 연결과 공식 upstream 운영 문서 갱신
- merge 뒤 default branch에서 실제 v0.8.4 draft 후보를 한 번 생성해 Issue #24에 전달하는
  post-merge Issue close gate

### 제외

- 자동 생성 PR의 자동 merge 또는 자동 승인
- `v0.8.4` source diff의 제품 수용 판정, adapter 보정과 Windows/Linux native 검증 —
  Issue #24 소유
- 자동 PR을 하이퍼-워터폴 task 최종 PR로 직접 merge하는 예외
- upstream source 직접 수정과 known issue backport
- `devel → main` release PR, `v0.1.0` tag, GitHub Release와 공개 asset 생성
- Windows/Linux package 공개, signing, updater 활성화와 Pages 배포
- macOS build·CI·검증·배포
- 기존 Alhangeul GitHub App private key를 저장소·로그·작업 문서로 복사
- repository 전체 Actions default permission을 write로 변경하거나 Actions의 PR 승인 권한 활성화

## 설계 방향

### Stable release와 provenance 판정

- latest는 GitHub Releases API의 latest Stable 응답을 사용하고 `draft=false`,
  `prerelease=false`, exact `vX.Y.Z`를 다시 검증한다.
- 수동 target tag도 release-by-tag 응답을 조회해 같은 Stable 조건을 적용한다. 존재하는
  Git tag만으로 공개 Stable로 인정하지 않는다.
- release `target_commitish` 문자열을 resolved commit으로 신뢰하지 않는다.
  `git ls-remote`의 `refs/tags/<tag>`와 annotated tag의 `^{}` ref를 해석해 40자리 commit을
  얻고, 갱신 script가 fetch 뒤 같은 commit을 재검증한다.
- current 비교는 `rhwp-core.lock`의 tag·commit을 기준으로 하며, Studio source는 submodule
  gitlink가 그 commit과 일치하고 exact upstream entry 계약이 통과해야 current로 판정한다.

### helper와 파일 크기 경계

- 282 LOC인 `scripts/update-upstream.sh`와 300 LOC를 넘은 기존 test는 확대하지 않는다.
- release resolve/classify, 관리 참조 치환, PR body 생성은 역할별 Node helper와 별도 test로
  분리하고 파일 300 LOC, 함수 50 LOC 권장 상한을 지킨다.
- workflow는 orchestration만 소유하고 release parsing·본문 escaping·치환 규칙을 긴
  inline shell로 중복하지 않는다.
- current pin 문구는 경로·marker·예상 횟수가 모두 일치할 때만 바꾸며 전역 문자열 치환을
  금지한다. release별 known issue 설명은 자동으로 다른 version 이름으로 바꾸지 않는다.

### 자동 PR과 하이퍼-워터폴 경계

- release별 bot-owned branch와 draft PR을 최대 하나 유지한다. 동일 tag의 열린 PR이 있으면
  source history를 rewrite하지 않고 기존 URL을 보고한다.
- merged PR 뒤 남은 branch, 사람이 수정한 branch, branch만 있고 PR이 없는 상태는 자동
  삭제·덮어쓰지 않고 blocker로 보고한다.
- 자동 PR은 merge 가능한 최종 task PR이 아니라 기계 생성 candidate다. Issue #24는
  `local/task24`에서 candidate 변경을 검토·가져오고 계획·native 검증을 완료한 뒤
  `publish/task24` PR을 만든다. 사용한 자동 PR은 별도 승인으로 닫고 branch를 정리한다.
- PR 본문은 자동 검증 성공과 Windows/Linux native 수용 완료를 구분하며 Issue #24를
  자동 close하지 않는다.

### credential과 외부 상태 변경

- resolve·build는 `GITHUB_TOKEN`의 read-only scope로 수행한다. write 직전 job에서만
  `actions/create-github-app-token`으로 installation token을 발급하고 contents·pull request
  최소 권한을 명시한다.
- 기존 Alhangeul Automation GitHub App을 Tauri 저장소에 설치하고
  `ALHANGEUL_AUTOMATION_CLIENT_ID`, `ALHANGEUL_AUTOMATION_APP_PRIVATE_KEY`를 설정하는
  방식을 권장한다. 현재 두 값은 Tauri 저장소에 없으므로 실제 등록은 별도 승인 gate다.
- GitHub App을 쓸 수 없을 때만 `GITHUB_TOKEN` 대안을 다시 승인받는다. repository-wide
  Actions PR 생성·승인 설정을 계획 단계에서 임의로 켜지 않는다.
- workflow·helper merge 전에는 default branch의 schedule과 `workflow_dispatch`가 활성화되지
  않는다. task PR 전에는 helper의 local read-only dry-run과 workflow 계약 test로 no-write
  경로를 검증하고, 실제 draft PR 생성은 task PR merge와 credential 설정 뒤 별도 승인으로
  dispatch한다.

### trigger, concurrency와 실패 정책

- daily schedule은 macOS workflow와 겹치지 않는 UTC 시각으로 분산하고 수동 입력은
  `target_tag`, `dry_run`만 둔다.
- repository·target tag 단위 concurrency는 중복 writer를 직렬화하고 running job을
  취소하지 않는다.
- update/build/check가 하나라도 실패하면 commit·push·PR 생성 전에 중지한다. 실패 시
  자동 reset, branch 삭제, force push를 수행하지 않고 step summary에 실패 단계를 남긴다.
- workflow가 만든 임시 artifact는 provenance 진단용 최소 범위와 짧은 retention만 사용하며
  release asset으로 해석하지 않는다.

## 문서 위치 판단

| 파일 | 분류 | 대상 독자 | 선택 위치 | 대안 위치 | 선택 이유 |
|---|---|---|---|---|---|
| `docs/architecture/UPSTREAM.md` | 공식 upstream 운영·아키텍처 문서 | 기여자/유지관리자 | `docs/architecture/` | `mydocs/manual/` | pin·갱신 진실 원천의 자동 감시·candidate 경계를 기존 문서에 이어 기록한다. |
| `docs/DEVELOPMENT.md` | 공식 개발·운영 명령 문서 | 기여자/유지관리자 | `docs/` | `README.md` | dry-run·수동 dispatch와 credential 준비 절차는 사용자 제품 소개보다 개발 문서 책임이다. |
| `mydocs/working/task_m010_23_stage*.md` | 실행·검증 증적 | 작업지시자/내부 작업자 | `mydocs/working/` | `docs/` | branch SHA, workflow run과 credential 상태는 특정 task 실행 기록이다. |
| `mydocs/report/task_m010_23_report.md` | 최종 작업 보고 | 작업지시자/후속 Task #24 | `mydocs/report/` | `docs/` | 공개 사용자 문서가 아닌 승인·handoff 기록이다. |

새 공식 문서 파일을 만들지 않고 기존 UPSTREAM·DEVELOPMENT 책임 안에서 필요한 절만
보정한다. GitHub App private key 값, token과 개인 식별 credential은 어느 문서에도 쓰지 않는다.

## 예상 변경 파일

신규 후보:

- `.github/workflows/rhwp-upstream-sync.yml`
- `scripts/check-rhwp-upstream-release.mjs`
- `scripts/update-rhwp-managed-references.mjs`
- `scripts/write-rhwp-sync-pr-body.mjs`
- `tests/rhwp-upstream-release.test.mjs`
- `tests/rhwp-managed-references.test.mjs`
- `tests/rhwp-sync-pr-body.test.mjs`
- `tests/rhwp-upstream-sync-workflow.test.mjs`

수정 후보:

- `package.json`
- `README.md`
- `docs/architecture/UPSTREAM.md`
- `docs/DEVELOPMENT.md`
- `apps/studio-host/src/core/upstream-boundary.test.ts` — 실제 제품 pin의 tag·commit 계약만
- `tests/rhwp-pin.test.mjs` — 자동 갱신 대상 exact pin fixture만

기존 `scripts/update-upstream.sh`와 `tests/actions-workflows.test.mjs`는 원칙적으로 수정하지
않는다. 구현 조사에서 불가피한 변경이 확인되면 구현계획서에 이유·LOC 영향을 기록하고
별도 승인을 받는다.

`tests/rhwp-pin-fetch.test.mjs`의 독립 fixture, `local-fonts.test.ts`의 v0.8.2 역사적 계약명,
UPSTREAM·DEVELOPMENT의 v0.8.2 known issue 문구와 `DESKTOP_RELEASE.md`의 Task #13 수용 기록은
현재 제품 pin이 아니므로 자동 치환 대상에서 제외한다.

이번 task 산출물:

- `mydocs/orders/20260812.md`
- `mydocs/orders/20260813.md`
- `mydocs/plans/task_m010_23.md`
- `mydocs/plans/task_m010_23_impl.md`
- `mydocs/working/task_m010_23_stage{N}.md`
- `mydocs/report/task_m010_23_report.md`

## 잠정 단계

- **Stage 1 — Stable release 판정과 관리 참조 계약**
  - release metadata·resolved tag commit을 검증하는 read-only helper와 fixture test를 만든다.
  - 기계 관리 가능한 current pin 참조의 allowlist·예상 치환 횟수와 회귀 test를 확정한다.
- **Stage 2 — draft sync PR workflow와 본문 생성**
  - thin workflow, PR body helper와 독립 workflow 계약 test를 추가한다.
  - no-drift·dry-run·existing PR·blocker·new candidate 분기와 secret preflight를 구현한다.
- **Stage 3 — 공식 문서와 플랫폼 중립 통합 검증**
  - UPSTREAM·DEVELOPMENT에 운영·보안·handoff 경계를 반영하고 전체 중립 gate를 실행한다.
  - local helper dry-run에서 v0.8.4 drift, tag·commit과 repository no-write 결과를 확인한다.
- **Stage 4 — task PR과 post-merge activation handoff 확정**
  - 최종 보고서에 GitHub App 설정 절차, merge 뒤 dispatch와 Issue close 조건을 고정한다.
  - task PR은 Issue #23을 자동 close하지 않으며 live candidate 성공 전 구현과 운영 수용을 구분한다.

## 검증 계획

### 단계별 검증

- Stage 1
  - helper unit/fixture test
  - latest Stable, explicit Stable, draft, prerelease, malformed tag, annotated/lightweight tag,
    no-drift와 mismatch test
  - 허용 경로·marker·치환 횟수 불일치·known issue 무손실 test
  - `git diff --check`
- Stage 2
  - workflow parse/contract test
  - `workflow_dispatch`+schedule, read/write job 분리, concurrency, draft PR, no auto-merge,
    no force push와 App token 최소 권한 검사
  - PR body의 provenance·changed paths·검증·native 미완료·Issue #24 handoff escaping test
  - `pnpm run test:automation`
  - `git diff --check`
- Stage 3
  - `pnpm install --frozen-lockfile`
  - `pnpm run check:product-boundary`
  - `pnpm run check:product-version`
  - `pnpm run check:release-metadata`
  - `pnpm run check:rhwp-pin`
  - `pnpm run test:automation`
  - `pnpm run test:upstream`
  - `pnpm run test:studio`
  - `pnpm run build:studio`
  - helper local dry-run의 `v0.8.4` drift·resolved commit·repository no-write 결과
  - `git diff --check`
- Stage 4
  - 최종 플랫폼 중립 gate 재실행과 task 최종 보고서 검토
  - task PR이 Issue #23을 자동 close하지 않는지 확인
  - post-merge dispatch·멱등 재실행·Issue #24 handoff checklist 확인

### merge 후 Issue close gate

- Task #23 PR이 `devel`에 merge되고 workflow 파일이 default branch에 존재한다.
- 승인된 GitHub App installation과 repository variable·secret 설정을 이름만 확인한다.
- `workflow_dispatch(target_tag=v0.8.4, dry_run=false)`가 성공하고 draft PR 하나를 만든다.
- draft PR의 base/head, old/new tag·commit, changed paths와 자동 검증 결과를 확인한다.
- 같은 입력을 반복해 추가 branch·PR·commit이 생기지 않음을 확인한다.
- token·secret이 log, artifact, step summary와 PR body에 없음을 확인한다.
- 결과를 Issue #23에 기록하고 #24에 candidate PR을 연결한 뒤에만 Issue #23을 close한다.

### 통합 검증

- latest Stable과 current pin이 같으면 repository write가 전혀 없다.
- drift가 있으면 동일 release core·Studio source candidate draft PR이 정확히 하나 생성된다.
- candidate 생성 전 플랫폼 중립 gate가 모두 통과하며 실패 시 push·PR이 생기지 않는다.
- automation PR은 native 검증·merge 완료로 오인되지 않고 Issue #24의 입력으로만 전달된다.
- 자동 merge, approval, release, tag, package publish, Pages deploy가 발생하지 않는다.
- PR 준비 전 `git status --short`가 빈 출력이다.
- `git diff --check`가 경고 없이 통과한다.

## 리스크

- **자동 PR과 task PR 혼동**: bot PR을 draft candidate로 표시하고 Issue #24가 정규 branch와
  계획·보고 절차로 변경을 수용하게 한다.
- **GitHub App 미설정**: read-only dry-run은 가능하지만 write gate는 실패한다. App 설치와
  variable·secret 등록을 별도 승인하고 preflight에서 명확히 중지한다.
- **과도한 workflow 권한**: read job과 write job을 분리하고 App token은 push 직전에만
  contents·pull request 권한으로 발급한다. Actions PR 승인 설정은 켜지 않는다.
- **release/tag spoofing**: Releases Stable metadata와 dereferenced Git tag commit을 모두
  검증하고 갱신 script가 fetch 뒤 재확인한다.
- **혼합 core·Studio release**: submodule, Cargo lock, WASM, pin과 exact Studio entry를
  한 commit에서 검사하고 일부만 성공한 변경은 push하지 않는다.
- **기계 치환의 문서 훼손**: allowlist, marker와 exact count를 사용하고 release별 known issue
  서술은 자동 치환하지 않는다.
- **bot branch 덮어쓰기**: 기존 branch·PR을 자동 삭제하거나 force-push하지 않고 사람이
  수정했거나 branch-only 상태면 blocker로 보고한다.
- **default branch 전용 dispatch**: task PR 전에는 helper local dry-run만 검증하고 실제
  scheduled/write 수용은 merge 뒤 Issue close gate로 남긴다. task PR은 Issue #23을 자동
  close하지 않는다.
- **GitHub API/네트워크 장애**: current로 오판하지 않고 workflow를 실패시키며 다음 schedule
  또는 수동 재실행으로 복구한다.

## 승인 요청 사항

- 하나의 daily schedule+manual workflow에서 read-only 판정과 App-token write job을 분리하는 방향
- exact Stable release metadata와 dereferenced Git tag를 함께 검증하는 provenance 계약
- 기존 update script를 확대하지 않고 세 helper·독립 test로 역할과 LOC를 분리하는 방향
- current pin의 기계 관리 참조만 allowlist로 갱신하고 release별 설명은 사람 검토에 남기는 경계
- 자동 branch/PR을 draft candidate로 만들고 Issue #24가 정규 task 절차로 수용하는 경계
- 기존 Alhangeul Automation GitHub App 재사용을 권장하되 설치·variable·secret 등록은 별도
  외부 상태 승인으로 두고 Actions PR 승인 설정은 변경하지 않는 보안 방향
- task PR 전 local read-only dry-run, merge 뒤 승인된 live v0.8.4 candidate 생성이라는
  2단계 검증과 Issue #23 지연 close
- UPSTREAM·DEVELOPMENT만 공식 문서로 보정하고 task 실행 증적은 `mydocs`에 두는 위치 판단
- auto merge, release/tag, native 수용, Issue #24 구현과 공개 배포를 제외하는 범위
- 위 4개 Stage와 단계별 승인 gate

승인되면 `task_m010_23_impl.md`에서 단계별 산출물, 검증 명령, 커밋 메시지를 구체화한다.
