# Task #64 수행계획서 — 변경 범위별 검증 선택과 전체 수용 경로를 통합한다

GitHub Issue: [#64](https://github.com/postmelee/alhangeul-tauri/issues/64)
마일스톤: M010

## 목적

작업별 검증 선택을 반복 가능하게 운영한다.

## 배경

Windows artifact 기준 실행 34047889263의 제품 build는 24:51, 34052696931은 34:37이다. 빠른 검사와 제품 생성, 기존 제품 검증을 분리할 근거가 있다. 진행 중인 #19/#57은 참조만 하고 미완료 소스는 통합하지 않는다.

## 범위

### 포함

- 검증 profile, 변경 경로 선택 규칙, 재사용 workflow, 플랫폼 의존관계를 통합한다. 모호한 변경은 보수적으로 확대한다.
- 이슈별 계획·검증·단계 보고 및 #59 통합 PR.
- nonpublishing Windows/Linux GitHub-hosted 검증.

### 제외

- 제품 기능·upstream 수정, release/서명/게시, PR merge/이슈 close.
- #27 action pin 정책과 #28 branch protection 변경.
- 주 worktree와 #19/#57 작업 변경.

## 설계 방향

- #60 계약 → #61 빠른 검사 → #62 artifact 재사용 → #63 core/cache → #64 통합 순서.
- parent local/task59의 독립 worktree에서 자식별 local/taskN 브랜치를 origin/devel 기준 생성하고 선행 CI 작업만 fast-forward로 포함한다. 자식 완료 commit을 parent에 fast-forward 통합한다. 제품 의존 작업의 미완료 branch는 포함하지 않는다.
- 동일 workflow에 대한 중복 PR을 피하려고 publish/task59 → devel 통합 PR 1개를 생성한다. 자식별 commit/계획/보고를 보존한다.
- 성공한 일부 계층은 전체 제품 수용이 아니다. 실패·건너뜀·재사용·미검증을 명시한다.
- 기존 대형 workflow/test는 이번 분리 경계와 직접 관련된 부분만 수정한다. 신규 모듈은 300 LOC/함수50 LOC를 목표로 한다.

## 문서 위치 판단

| 파일 | 분류 | 대상 독자 | 선택 위치 | 대안 위치 | 선택 이유 |
|---|---|---|---|---|---|
| CI_VALIDATION.md | 공식 운영 문서 | 기여자·에이전트 | docs/operations/CI_VALIDATION.md | mydocs/manual | 프로젝트 고유 CI 계약의 지속적 진실 원천 |
| 진입 링크 | 기존 개발/운영 지침 | 기여자·에이전트 | docs/DEVELOPMENT.md, AGENTS.md, docs/operations/RELEASE_CHECKLIST.md | 새 중복 가이드 | 필요한 검증 선택 가이드로 연결 |
| 계획·단계·최종 근거 | 작업 산출물 | 작업지시자 | mydocs/plans, working, report | 공식 가이드 본문 | 개별 실행 기록은 운영 계약과 분리 |

2026-09-07 이 side conversation의 전체 구현 승인에 문서화 및 해당 위치 결정을 포함하여 기록한다.

## 예상 변경 파일

- .github/workflows/ci.yml, alhangeul-desktop.yml 및 검증 계층별 reusable workflow
- scripts/ci/*, tests/ci-*.test.mjs, package.json, 관련 기존 workflow 계약 test
- 위 공식 가이드·진입 링크
- mydocs/orders/20260907.md
- mydocs/plans/task_m010_64.md, task_m010_64_impl.md
- mydocs/working/task_m010_64_stage1.md ~ stage3.md
- mydocs/report/task_m010_64_report.md

## 잠정 단계

- Stage 1 — 계약과 구현: 소유 경계·입력·출력·실패 규칙 정의 및 해당 계층 구현.
- Stage 2 — 회귀 검증: positive/negative fixture와 source 계약 검사, 지원 runner 검증 준비.
- Stage 3 — 통합 수용: #59 통합 후보에서 실제 CI 결과와 측정치 확인, 보고·PR 준비.

## 검증 계획

### 단계별 검증

- Stage 1: git diff --check 및 해당 Node 계약 검사.
- Stage 2: pnpm run test:automation, product-boundary/version/release metadata, test:upstream/studio, build:studio 및 CI 선택 테스트.
- Stage 3: GitHub-hosted Windows/Linux에서 실제 workflow run/job/step 증거 수집. 무게시 artifact 생성 및 재사용 canary.

### 통합 검증

- 선택 규칙의 회귀 검증, 전체 수용 경로 유지, 운영 가이드 및 진입 링크가 일치한다.
- 로컬에서는 중립 Node 검사만 수행한다. Rust desktop/Tauri/PowerShell native 실행은 Windows/Linux CI에 한정한다.
- 캐시 hit≠컴파일 재사용, 다른 workload의 시간≠직접 속도 개선임을 기록한다.
- PR 생성 전 tracked/untracked 작업 상태와 git diff --check 확인.

## 리스크

- 분리 작업 중 필수 gate 누락: 기존 실제 native/installer gate 유지 + 구조 계약 회귀 검사.
- artifact 혼동: exact product/harness SHA 및 ID/digest·inventory 검증, 누락/만료/불일치 거부.
- 오래된 캐시: compiler/target/lock 경계 + 소스 SHA 갱신; cold/warm을 별도 측정.
- #19/#57 후속 병합 충돌: 기존 소스 소유 경계 보존 및 통합 유의사항 문서화.

## 승인 요청 사항

추가 요청 없음. 작업지시자가 이 side conversation에서 이슈 등록부터 모든 하위 이슈 구현·검증·PR 생성까지 추가 승인 없이 진행하도록 2026-09-07 명시 승인했다. 새로운 제품/게시/권한 범위로 확장하지 않는다.

