# Task #57 수행계획서 — Windows 썸네일 설치 환경별 진단·대응

GitHub Issue: [#57](https://github.com/postmelee/alhangeul-tauri/issues/57)
마일스톤: M010
상태: Stage 3.2 종료 코드 보완 후보·로컬 검사 완료 — Windows 재실행 승인 대기

승인 근거: 2026-09-06 같은 스레드에서 수행계획서 승인 요청에 작업지시자가
“진행해줘”로 구현계획 작성을 지시했다. 이어 구현계획 승인 요청에도 “진행해줘”로
Stage 1 진행을 승인했다. Stage 2 후보의 게시·실행도 별도로 승인받아
2026-09-07 Windows-only run `34042095817`을 시작했다.

동일 run은 빌드 성공, NSIS/MSI smoke 실패로 종료됐다. NSIS는 문서 Shell 요청의
`0x80040154`, MSI는 썸네일 성공 뒤 강제 재설치의 `3010`이 실패 원인이다.
실측 환경은 `EnableLUA=1`이므로 UAC 값만으로 제한을 판단하지 않는다.
상세 근거와 후속 승인안은 구현계획서의 “Stage 2.1 원격 관측 결과”에 기록한다.
이는 증상 재현 기록이며 Stage 2 수용·현장 해결 완료를 뜻하지 않는다.

실측 보고 뒤 “진행해줘” 지시로 보완 계획 작성을 승인받았다. 구현계획서에
Stage 2.2 진단/제품 수용 분리, MSI 일반 재설치/강제 교체 격리, 재부팅 상태 수집과
Stage 3 안내 방향을 구체화했다. 이어 같은 스레드의 “진행해줘”로 Stage 2.2의
진단/제품 수용 분리와 구현·로컬 검증·후보 커밋을 승인받았다. 이후 후보
`001cc3adeab003260fb7b830f6f757b53c65d489`의 게시·Windows-only 추가 1회 실행도
별도 승인받아 run `34047889263`에서 검증했다. 세 installer job의 진단 계약 검사는
통과했고 MSI 일반 lifecycle도 통과했다. NSIS 썸네일과 MSI 강제 교체의 제품 실패는 유지한다.
분석 뒤 “진행해줘” 지시에 따라 [Stage 2 보고서](../working/task_m010_57_stage2.md)를
작성했다. 진단 단계 완료와 #57 전체 완료는 다르며 Stage 3 구현은 아직 착수하지 않았다.

Stage 2 보고 뒤 “진행해줘” 지시로 Stage 3의 제공 방식·안내 세부계획을 작성했다.
구현계획서에 단일 문서+JPG 수동 진단, 같은 빌드의 독립 지원 artifact와 해시 검증,
원본/개인정보 보호, 기존 문서 3개의 위치 판단, Windows 패키지 실사용 검증을 명시했다.
이는 구현 승인 요청안이다. installer/등록/엔진 변경·앱 설정·#58·공개 릴리즈는 포함하지 않는다.

2026-09-07 세부계획 후보 `fd046ad` 제시 후 같은 스레드의 “진행해줘”로
Stage 3 구현·로컬 검사·후보 커밋을 승인받았다. 단일 문서/JPG 수동 도구, 별도 지원 묶음,
CI 패키지 실사용 gate와 MSI 안내를 구현했다. 플랫폼 중립 검사 102개·전체 automation
558개와 product boundary·actionlint·diff 검사를 통과했다. Windows PowerShell/native·
실제 묶음 생성은 실행하지 않았다. 상세는 구현계획서의 Stage 3.1 기록을 따른다.
이는 Stage 3 전체 수용이 아니다. exact 후보 SHA의 추가 Windows-only 비게시 1회 실행은
별도 승인받으며 installer·등록·엔진·#58·릴리즈 범위는 변경하지 않았다.

이후 후보 `b4fa125` 게시·Windows-only 1회 실행을 승인받아 run `34052696931`을
수행했다. build와 지원 묶음 생성은 통과했지만 수동 진단 테스트가 통과 메시지 뒤
비정상 종료해 실제 installer/Shell 검사는 생략됐다. 실패 분석 뒤 “진행해줘”로
종료 코드 수정·회귀 검사·로컬 검증·후보 커밋을 승인받았다.
Stage 3.2에서 정상 끝의 명시적 성공 종료와 실제 테스트 process 종료 계약 검사를 추가했다.
플랫폼 중립 대상 104개·전체 560개, product boundary·actionlint·diff 검사를 통과했다.
새 Windows process 회귀는 미실행이며 원격 재실행은 별도 승인 대상이다.
제품 진단 도구·설치·엔진 코드는 바꾸지 않았고 Stage 3 전체 수용은 아직 완료하지 않았다.

## 목적

같은 Windows PC에서 NSIS 설치는 썸네일에 실패하고 MSI 설치는 성공한 현상을
설치 범위·COM 실행 문맥·캐시·설치 순서별로 분리해 조사한다. 먼저 GitHub Actions의
깨끗한 Windows VM을 활용하고, hosted 환경에서 설명되지 않는 조건만 승인된
Windows 10/11 VM·VDI에서 보완한다. 물리 PC 확보를 시작 조건으로 두지 않는다.

등록 명령 성공, 처리기 선택, 직접 COM 생성, 실제 Shell bitmap 반환을 구분하고
지원 환경·제한·설치 대안을 근거로 정한다. 후속 #58의 선택 설치·앱 내 토글 설계에
필요한 등록·권한·복원 경계를 인계한다.

## 배경

- 사용자 보고 artifact: [run 34021920074 / artifact 9986364323](https://github.com/postmelee/alhangeul-tauri/actions/runs/34021920074/artifacts/9986364323).
  한글 2022가 설치된 PC에서 NSIS는 작은/큰 HWP 모두 실패했으나 제거 후 같은 빌드의
  MSI를 설치하자 둘 다 성공했다. 한글 2024가 설치된 VDI에서는 NSIS도 성공했다.
- 문제 PC의 HWP/HWPX 처리기 조회는 Alhangeul CLSID, 직접 COM 생성은 OK,
  Shell GetImage는 `0x80040154 / Bitmap=False`였다. JPG는 `0 / Bitmap=True`였다.
  DLL·worker 및 사용자별 COM 등록이 존재했고 `EnableLUA=0`, 관리자 권한이었다.
- 현재 NSIS는 64비트 HKCU, MSI는 64비트 HKLM에 처리기를 등록한다. MSI 전환은
  등록 범위 외에도 설치 경로·프로세스·캐시 상태를 바꾸므로 단일 원인은 미확정이다.
- [Microsoft COM 등록 규칙](https://learn.microsoft.com/en-us/windows/win32/sysinfo/hkey-classes-root-key)은
  UAC가 꺼진 관리자 실행의 사용자별 등록 제한을 설명한다. 그러나
  [GitHub Windows runner도 관리자 권한·UAC 비활성화](https://docs.github.com/en/actions/reference/runners/github-hosted-runners#administrative-privileges)이며
  기존 CI는 성공했다. UAC 값만으로 원인을 확정하거나 NSIS를 일괄 차단하지 않는다.
- 현재 installer smoke는 같은 job에서 MSI → 제거 → NSIS 순서다. 선행 설치의
  등록·프로세스·캐시 영향은 가설로만 두고 NSIS-only clean job으로 분리한다.
  현재 Shell interop은 bitmap을 받아 해제하지만 호출자에게 HRESULT만 반환한다.
  성공 HRESULT와 non-null bitmap, 실제 새 추출을 별도로 검증할 필요가 있다.
- 시작 기준은 최신 `origin/devel`의 `c93ac8c58a796a45227f764f37b7aaffaa81899e`다.
  메인 worktree의 `local/task19`는 건드리지 않고 `.claude/worktrees/task57`의
  `local/task57`에서 수행한다. #19의 미통합 변경은 본 작업 근거에 섞지 않는다.

## 범위

### 포함

- Windows x64 NSIS-only/MSI-only의 독립 clean job, 기존 smoke·rollback·복원 검사 재사용
- source SHA·installer hash·runner image와 OS build·실행 권한·세션·registry view의 증거 연결
- HKCU/HKLM 등록, 연결 조회, 직접 COM 생성, Shell GetImage·bitmap을 구분하는 공통 진단
- 작은 HWP·큰 HWP·HWPX 및 JPG 대조군, 캐시와 새 추출 구분, 원본 fixture 불변 확인
- 설치 직후·재설치·제거·rollback 및 제3자 등록 보존 검증
- 실측 실패 조건에 기반한 제한 감지·MSI 안내와 필요한 최소 설치 통합 보정
- 재현되지 않을 때의 Windows 10/11 로그인·Explorer 잔여 검증 계획과 #58 인계

### 제외

- 추측성 한글 버전별 분기, 기본 앱/UserChoice 강제 변경, 엔진·resource limit 변경
- #58의 선택 구성요소·앱 설정 구현, Preview Handler, Linux 기능 변경, Windows ARM64
- NSIS 전체 사용자 설치의 무조건 도입 또는 사용자 동의 없는 HKLM 전환
- UAC·보안 정책 자동 변경, 사용자 Explorer/dllhost 강제 종료, 전역 thumbnail cache 삭제
- upstream pin 변경, 사용자 문서·사진·민감 계정 정보의 저장소/공개 artifact 업로드
- 공개 릴리즈·서명·Pages·updater 활성화, 유료 VM·상시 self-hosted runner 무승인 구축

## 설계 방향

1. **진단을 먼저 고정한다.** 파일 존재와 COM 등록 명령만으로 성공 처리하지 않는다.
   연결 조회·직접 COM·Shell 요청은 독립된 결과로 남기며 순서가 상태에 영향을 줄 수
   있으므로 깨끗한 프로세스와 요청 순서도 기록한다. 초기 진단은 registry를 변경하지 않는다.
2. **설치 방식별로 VM을 분리한다.** 동일 검증 bundle을 받아 각각 선택한 installer만
   실행한다. install 이전의 양 hive·프로세스 상태를 확인해 오염된 환경을 정상으로 삼지 않는다.
   기존 순차 설치는 원인 비교가 필요할 때만 별도 대조군으로 두고 기본 수용 gate로 쓰지 않는다.
3. **캐시와 실제 호출을 구분한다.** task 소유 fixture 복사본과 새 경로를 사용하고 원본은
   수정하지 않는다. 새 경로만으로 cold extraction을 단정하지 않고 cache/extraction API의
   제어·관측 방법을 구현계획에서 정한다. JPG 성공도 모든 Shell 구성요소의 정상 증명은 아니다.
4. **오류를 숨기지 않는다.** HRESULT, bitmap 유무와 크기 등 관측값을 함께 수집한다.
   expected negative 검사는 별도로 표시하고 실패·증거 누락을 retry로 성공 처리하지 않는다.
   타임아웃과 설치 형식별 유일한 artifact 이름을 두어 진단 누락·충돌을 방지한다.
5. **환경에 맞춘 해결을 승인받는다.** NSIS-only가 실패하면 MSI-only 및 기존 순차 결과와
   비교하고 최소 보정안을 제시한다. hosted에서 모두 성공하면 Windows client/session 차이를
   조사한다. 등록 모델 변경이나 새 권한 helper 도입은 Stage 3 진입 전 구체적 승인을 받는다.
6. **사용자·기존 프로그램의 소유권을 보존한다.** 공유 key 전체 삭제를 하지 않고 기존
   snapshot/조건부 복원 계약을 유지한다. 계정 종류·권한을 기록하되 개인 계정·문서 경로는
   비식별화한다. 실제 한컴이 없는 가짜 ProgID 검사를 한컴 호환성 수용으로 표현하지 않는다.
7. **기존 경계를 유지한다.** #35의 GUI E2E와 도구를 재사용하되 editor/print 자동화를
   복제하지 않는다. helper는 역할별로 나누고 파일 300 LOC·함수 50 LOC를 목표로 한다.
   기존 대형 workflow·WiX 템플릿은 제한된 orchestration 수정만 하며 전면 리팩터링하지 않는다.

## 문서 위치 판단

기존 공식 문서 루트 `docs/`를 유지한다. 아래 제품 문서 변경은 본 계획 승인 뒤 수행하며,
이번 시작 커밋에서는 계획서와 오늘할일만 작성한다.

| 파일 | 분류 | 대상 독자 | 선택 위치 | 대안 위치 | 선택 이유 |
|---|---|---|---|---|---|
| `WINDOWS_THUMBNAILS.md` | 공식 아키텍처 | 기여자·통합 담당자 | `docs/architecture/` | `mydocs/tech/` | 확정된 등록·진단 계약은 기존 진실 원천에 반영 |
| `DESKTOP_RELEASE.md`, `RELEASE_CHECKLIST.md` | 공식 운영 문서 | 릴리즈 담당자 | `docs/operations/` | `mydocs/manual/` | 설치 환경 검증은 제품 운영 범위이며 방법론 매뉴얼과 분리 |
| `v0.1.0.md` | 버전별 근거 | 릴리즈 담당자 | `docs/releases/` | `docs/operations/` | 해당 버전의 run·한계·잔여 gate만 연결하고 공개 상태는 변경하지 않음 |
| `README.md` | 제품 진입 문서 | 사용자·기여자 | 저장소 루트 | 신규 FAQ | 필요한 경우 썸네일 제한·MSI 대안의 짧은 안내와 기존 문서 링크만 보완 |
| `task_m010_57*.md` | 작업 산출물 | 작업지시자·내부 작업자 | `mydocs/plans/`, `working/`, `report/` | `docs/` | 승인·가설·단계별 결과는 제품 계약과 분리 |

## 예상 변경 파일

신규 후보(정확한 분리는 구현계획에서 확정):

- `scripts/windows-thumbnail-diagnostics.ps1` — 상태 수집과 독립 probe orchestration
- `scripts/windows-thumbnail-interop.cs` — 필요한 경우 COM/Shell 관측을 분리
- `tests/windows-thumbnail-diagnostics.test.mjs` — 진단 계약·누락·실패 분류 회귀

수정:

- `.github/workflows/alhangeul-desktop.yml` — installer별 격리 job과 증거 연결
- `scripts/windows-installer-smoke.ps1`, `scripts/windows-installer-smoke-support.ps1`
- `scripts/windows-thumbnail-smoke.ps1`
- `tests/windows-installer-smoke.test.mjs`, `tests/windows-thumbnail-registration.test.mjs`
- `tests/windows-packaging.test.mjs`, `tests/actions-workflows.test.mjs`, `package.json`
- 조건부: `apps/desktop/src-tauri/windows/nsis-hooks.nsh`, `windows/main.wxs`
  (후자는 같은 `src-tauri` 아래), `apps/thumbnail-handler/src/registration/windows.rs`
  — Stage 2 실측 뒤 승인받은 최소 보정에 한정
- 위 문서 위치 판단에 기재한 기존 공식 문서

이번 task 산출물:

- `mydocs/orders/20260906.md`
- `mydocs/plans/task_m010_57.md`
- 승인 후 `mydocs/plans/task_m010_57_impl.md`
- `mydocs/working/task_m010_57_stage{N}.md`
- `mydocs/report/task_m010_57_report.md`

## 잠정 단계

- **Stage 1 — 증거·진단 계약 정리**
  - 기존 fixture·등록·Shell probe와 실패 지점을 정리하고 공통 진단·회귀 검사를 마련한다.
  - bitmap 반환, 민감 정보 배제, 오류와 미설정 값의 구분을 확인한다.
- **Stage 2 — Actions 격리 재현**
  - NSIS-only와 MSI-only clean job을 구성하고 같은 exact SHA의 실제 native run을 비교한다.
  - 진단·fixture hash·실행 문맥·실패/성공 결과를 read-back하고 가설별 결과를 보고한다.
- **Stage 3 — 근거 기반 대응과 조건부 client 검증**
  - Stage 2 결과로 최소 보정·제한 감지·MSI 안내안을 구체화하고 승인 후 적용한다.
  - hosted 미재현 또는 client 문맥 확인 필요 시 VM·VDI 대상/절차를 별도 승인받는다.
  - 환경이 없으면 해결됐다고 처리하지 않고 확인 불가 조건과 추가 수용 gate를 보고한다.
- **Stage 4 — 회귀 수용·문서·#58 인계**
  - 동일 최종 후보로 installer lifecycle·Shell 생성·기존 등록 보존 회귀를 확인한다.
  - 확정 조건·미확정 조건·NSIS 전체 사용자 설치 필요성·#58 권한 경계를 정리한다.

수행계획 승인 → 구현계획 작성·승인 → 각 Stage 수행·보고·승인의 순서를 유지한다.
단계 승인이 다음 단계 실행이나 새로운 환경·권한 확대까지 자동 승인하는 것은 아니다.

## 검증 계획

### 단계별 검증

- 시작 단계: `git diff --check`, 계획서 필수 섹션·#57 오늘할일 행·변경 파일 allowlist 확인.
  문서만 작성하므로 이번에는 native build나 workflow를 실행하지 않는다.
- Stage 1: 관련 Node source-contract 검사와 `pnpm run check:product-boundary`,
  `pnpm run test:automation`. 이 검사는 Windows COM 실제 실행을 대신하지 않는다.
- Stage 2: 승인된 `workflow_dispatch`의 Windows x64 artifact mode에서 native
  handler/worker test·Clippy·build, NSIS/MSI 설치·rollback·재설치·제거, 실제 Shell 요청.
  run ID·workflow/source SHA·installer hash·artifact read-back까지 확인한다.
  명령·dispatch ref·원격 게시 경로·실행 횟수는 구현계획과 해당 단계에서 승인받는다.
- Stage 3: 새 검출 조건의 positive/negative 검사, 필요하면 UAC on/off가 사전 적용된
  전용 Windows 10/11 VM에서 비교. 변경 뒤 재부팅 없이 UAC 상태 전환을 수용하지 않는다.
  실제 Explorer UI 성공, 직접 COM 성공, API bitmap 성공은 따로 표시한다.
- Stage 4: 관련 자동 검사와 플랫폼 중립 `pnpm run test:upstream`,
  `pnpm run test:studio`, `pnpm run build:studio`, 최종 Windows lifecycle 회귀.
  Linux native 재검증은 공유 build/protocol 변경이 있을 때만 범위를 추가 승인받는다.

### 통합 검증

- 서로 다른 installer 유형은 같은 source SHA의 해당 bytes로 각각 검증하고 이전 run 성공을
  새 파일에 승계하지 않는다. 실패 관측을 제외한 요약만으로 success를 주장하지 않는다.
- 작은/큰 HWP·HWPX의 Shell bitmap과 JPG 대조군을 기록하고 문서 bytes는 변하지 않는다.
- 설치·재설치·rollback·제거 뒤 기존 기본 연결·제3자 handler·제품 소유 자원이 일관된다.
- 원인 미확정이나 client 미검증은 해결 완료와 분리한다. 잔여 위험의 수용 여부는
  작업지시자가 판단하며 CI 성공만으로 현장 문제 해결·이슈 close를 선언하지 않는다.
- PR 준비 전 task worktree의 `git status --short`는 비어 있고 `git diff --check`는 통과한다.

## 리스크

- **호스트 차이**: Windows Server runner의 성공을 Windows 10/11 로그인 세션에 일반화하지 않는다.
- **설치 이력 오염**: MSI 제거 후 registry가 없어도 process/cache 상태가 남을 수 있어 독립 VM을 기본으로 한다.
- **원인 조기 확정**: UAC와 한글 버전은 환경 속성이지 단독 원인으로 확정된 값이 아니다.
- **개인정보**: 공개 fixture만 사용하고 사용자 문서·원본 사진·전체 registry dump를 artifact로 올리지 않는다.
- **병렬 작업**: #19 메인 worktree 및 #35 자동화 소유 범위에 비간섭. 같은 오늘할일 파일의
  추후 병합에서는 각 task 행을 보존하고 승인 없이 다른 브랜치를 통합하지 않는다.
- **범위 확대**: 전체 사용자 NSIS, 권한 helper, engine 변경이 필요하면 현재 단계에서 승인 요청한다.
- **과거 artifact 만료**: 기존 run은 역사적 증거로 남기고 만료된 파일 대신 새 빌드를 동일 bytes로 표현하지 않는다.

## 승인 요청 사항

1. #57을 위 4단계로 수행하고 Actions 설치 방식별 격리 검증을 우선한다.
2. 진단·검증을 먼저 보완하고, 제품 보정은 Stage 2 결과에 따른 구체적 승인 후 진행한다.
3. 기존 `docs/`와 루트 README의 제한된 문서 위치 판단 및 작업 산출물 배치에 동의한다.
4. client VM·VDI·self-hosted/유료 환경과 NSIS 전체 사용자 설치는 필요성 확인 후 별도 승인한다.
5. #58 구현, 릴리즈·서명·배포와 기본 앱·보안 정책 변경은 이번 승인 범위에서 제외한다.

승인되면 `task_m010_57_impl.md`에서 단계별 산출물, 명령, 커밋과 원격 실행 경로를 구체화한다.
