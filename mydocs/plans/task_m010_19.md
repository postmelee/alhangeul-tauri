# Task #19 수행계획서 — PDF snapshot과 stale job 회수 경계

GitHub Issue: [#19](https://github.com/postmelee/alhangeul-tauri/issues/19)
마일스톤: M010

## 2026-09-07 승인 — 수용 근거 계층화와 두 가지 최소 보완

남은 근거 검토 뒤 작업지시자가 `진행해줘`로 수용 기준 보정과 Windows 링크 보존·Linux HWPX
직접 PDF 보완을 승인했다. 제품 계약은 유지하되 snapshot/timeout/잠금은 기존 결정적 테스트를
주된 근거로 수용한다. 실제 동시 편집·WebView reload·장시간 대기·앱 재시작 cleanup의 통합
시나리오는 미실행 한계로 남긴다. 과거 Stage 4의 모든 경계를 실제 GUI로 확인한다는 실행
요구보다 이 보정이 우선하며, 미실행 시나리오를 통과로 고쳐 쓰지 않는다.

- Windows의 실제 junction과 대상 sentinel을 만들어 production cleanup의 보존을 검사한다.
- Linux의 기존 direct PDF 경로에 공개 HWPX를 추가해 10쪽/A4/한글 검색/내용/원본 보존을 검사한다.
- 기존 CI/Linux GUI workflow에 제한 실행 선택만 추가한다. Windows는 PDF cleanup Rust 검사,
  Linux는 기존 검증된 DEB의 HWPX PDF만 실행하며 패키지 재빌드·인쇄·썸네일·updater를 반복하지 않는다.
- 문서 독자는 릴리즈 담당자다. 공식 기준은 기존 `docs/operations/DESKTOP_RELEASE.md`를 수정한다.
  대안인 새 가이드/`mydocs/manual`은 사용하지 않는다. 승인 근거는 기존 plans, 실행 결과는
  `mydocs/working/task_m010_19_stage4.22.md`, 오늘할일은 기존 orders에 둔다.
- HWPX 표 조판 관측은 원인 미확정 위험으로 유지한다. 조판 수정·별도 이슈 생성·릴리즈 승인은 포함하지 않는다.

## 2026-09-07 후속 결과 — 4.19 전체 PDF 자동 회귀 검증 완료

run `34058443345`에서 Windows HWP/HWPX fresh/restart와 Ubuntu PDF 분석을 모두 통과했다.
보조 tree는 정상 실행에서 disabled이고 PS 회귀 16개도 통과했다. [4.19 보고서](../working/task_m010_19_stage4.19.md)에
네 저장 결과·원본 보존·32쪽 분석과 시각 한계를 기록한다. 기존 HWPX 표 조판 관측 및 #19의
동시 편집/reload/TTL 실제 수용은 별도로 남긴다. 동일 workflow 재실행·새 자동화 추가·PR/릴리즈는
수행하지 않고 남은 근거 검토 승인을 요청한다. 아래 승인·실패 이력은 당시 기록으로 보존한다.

후속 `진행해줘`로 4.19 실패의 최소 보정을 승인받았다. 일반 실행의 보조 tree를 비활성화하고
기존 관측-only 모드에 한정한다. 진단 직렬화와 실제 PS 회귀를 보완하며, 기존 PDF workflow의
설치 전 빠른 검사를 모든 mode에 적용한다. 동일 제품의 전체 경로 한 번으로 재검증하며
기존 문서 위치를 유지한다. 구현계획서의 4.19 보조 수집 분리 범위를 따른다.

4.21 완료 뒤 후속 `진행해줘`로 기존 제품의 HWP/HWPX fresh/restart PDF 회귀 검증을 승인받았다.
미완료 4.19를 재개하며 새 코드/빌드/테스트 구조는 추가하지 않는다. 기존 가이드·사건 기록·
plans/orders와 검증 완료 시 4.19 보고서에 결과를 기록한다. 상세는 구현계획서의 재개 범위다.

재개 run `34056914386`은 fresh 두 문서 저장 이후 HWP restart의 보조 tree metadata
수집 오류로 실패했다. 확인 버튼 호출 전 중단이며 HWPX restart/원격 분석은 미실행이다.
원본/기존 target 보존과 fresh PDF 부분 분석은 통과했다. [사건 기록](../troubleshootings/task_m010_19_windows_pdf_automation.md)에
근거를 남기고 당시 4.19 미완료·추가 보정/실행 승인 대기로 두었다. 정상 경로의 보조 tree 수집을
명시적 진단 mode로 한정하는 권고는 위 후속 승인의 보정 대상이다. 4.21의 제한 통과는 유지한다.

4.20 결과 뒤 작업지시자의 `진행해줘`로 **Stage 4.21 native 확인창 호출 보정·실제 앱 제한
검증**을 승인받았다. 같은 #19/브랜치와 기존 제품 bytes를 유지한다. 확인·거절·파일 보존만
대상으로 하며 전체 PDF/제품 재빌드는 하지 않는다. 문서 독자는 자동화 유지보수자이고,
공식 사용 기준은 기존 `docs/operations/NATIVE_UI_TESTING.md`, 사건/계획/단계 결과는 기존
`mydocs` 경로에 둔다. 새 공식 문서나 `manual`의 제품 문서 추가 대신 기존 위치를 갱신한다.
세부 구현/검증 범위는 구현계획서의 4.21을 따른다.

4.21 run `34053001644`에서 실제 거절·잘못된 target 거부와 파일 보존은 통과했다.
Confirm은 호출 전에 보조 화면 트리 수집의 `ElementNotAvailableException`으로 중단했다.
완료 처리하지 않고 [사건 기록](../troubleshootings/task_m010_19_windows_pdf_automation.md)에
부분 통과·원인·보존 근거를 남긴다. 후속 `진행해줘`로 보조 진단 예외 격리와 남은 Confirm-only
검증을 같은 4.21 안에서 승인받았다. 구현계획서의 후속 승인 범위에 따라 진행한다.

후속 run `34055921568`은 **8분 55초 통과**했다. 실제 Confirm·PDF 교체·원본/다른 target
보존과 cleanup, PS5.1 합성 예외 회귀 10개를 확인했다. [4.21 보고서](../working/task_m010_19_stage4.21.md)에
이전 두 거절과 이번 확인을 서로 다른 실행의 부분별 근거로 묶는다. 제품 bytes/클릭 adapter는
같으며 거절 사례·제품 빌드를 반복하지 않았다. 4.21만 완료하고 4.19/#19 전체 수용은 남긴다.

후속 `진행해줘`로 **Stage 4.20 실제 앱 확인창 최소 진단**을 승인받았다. 4.19의 전체 수용
실패는 유지한다. 기존 제품 한 번 설치·공개 HWP 한 개 열기·시험 target 저장 요청으로 확인창만
관측한다. Yes/No 호출·제품/PDF 재빌드·전체 수용 반복은 제외한다. 기존 공식 가이드/사건 기록과
plans/orders/working 위치를 유지하며 새 공식 문서는 만들지 않는다. 상세는 구현계획서에 둔다.

4.20은 run `34051827068`에서 진단 목적을 완료했다. 실제 확인창의 UIA Invoke 미지원,
native `Button`/활성 상태·13개 guard 통과, 숫자 ID 조회 반환값 0을 확인했다.
버튼은 호출하지 않았고 source/시험 target 보존과 cleanup이 통과했다. [4.20 보고서](../working/task_m010_19_stage4.20.md)의
관측 기반 native 호출 adapter·실제 앱 제한 검증은 다음 승인 범위이며 4.19 전체 수용은 미완료다.

작업지시자가 Stage 4.16 다음 진행을 지시했다. 기존 #19와 `local/task19`를 유지한다.
작업지시자가 후속 `진행해줘`로 아래 Stage 4.17의 구현과 작은 Windows 검증을 승인했다.
Stage 4.17 보고 뒤 작업지시자가 다시 `진행해줘`로 4.18 확인창 adapter·작은 통합을 승인했다.
4.19 전체 PDF 수용은 작은 통합 결과 보고 뒤 별도 승인한다.
Stage 4.18은 `d553f8e`의 run `34049005561`에서 정책/진단 72개와 작은 통합 5개를 통과했다.
실제 제품/PDF 검증은 아니다. [4.18 보고서](../working/task_m010_19_stage4.18.md)를 기준으로
작업지시자가 후속 `진행해줘`로 4.19의 기존 설치본 전체 PDF 검증을 승인했다.
artifact 유효성을 확인했으며 제품 재빌드 없이 현재 adapter로 한 번 실행한다.

4.19 실행 `34049930142`는 fresh 두 문서 저장·원본/dirty 보존을 통과했으나 HWP restart의
`Confirmation InvokePattern unsupported.`로 실패했다. 실제 확인창은 UIA Pane이며 지원
pattern 목록이 비어 있다. HWPX restart·원격 전체 PDF 분석은 미실행이다. 새 저장 PDF 16쪽의
별도 로컬 분석과 시각 관측은 [사건 기록](../troubleshootings/task_m010_19_windows_pdf_automation.md)에
구분해 남겼다. 4.19는 미완료이며 helper 보정·원격 재실행은 추가 승인 전 수행하지 않는다.

### 목적과 근거

정적 문자열 검사와 긴 PDF E2E 사이에 실제 판단 함수·작은 OS 검증을 둔다. 계획 수립 당시의
`Find-NativeButton`은 UIA 탐색과 판단이 결합되어 있고, overwrite는 ID `6`/class
`Button`만 실행한다. 관측된 `CommandButton_6/7`에는 안전한 실행 adapter가 없다.
기존 진단은 prompt 의미·소유 관계·지원 pattern을 충분히 기록하지 않아 ID만 바꾸는
수정으로 해결을 보장할 수 없다. 현재 Mac 호스트에 PowerShell 실행기도 없으므로
Node source 계약을 PowerShell 동작 검증으로 대체하지 않는다.

### 포함·설계 방향

- **판단과 OS 조작 분리**: 순수 PowerShell 함수에 관측 DTO와 명시한 기대 조건을 전달한다.
  파일/프로세스/창을 조회하거나 클릭하지 않는 함수이며 실제 dialog helper가 이를 호출한다.
  UIA adapter는 조회를, 기존 Win32 adapter는 검증된 조작을 담당한다.
- **선택 계약**: 앱/대화상자 범위, ID/class, enabled, 중복·누락을 검사한다. UIA tree의
  파일 항목과 버튼 ID 충돌을 재생한다. 순수 함수가 반환한 대상은 클릭 직전 실제 UI에서
  다시 검증하여 stale element나 다른 dialog를 승인하지 않는다.
- **확인창 계약**: Save 의도·기존 테스트 target·직전 제출 경로·저장창/확인창 소유 관계·
  prompt 의미와 대상 일치를 확인한다. 미지의 형태·다른 파일·Open 중 modal은 거부한다.
  이 조건의 통과와 실제 버튼 호출 성공·파일 갱신은 별도 결과다.
- **실제 함수 테스트**: 같은 PowerShell 함수를 dot-source하는 소규모 assertion script를
  Windows PowerShell 5.1에서 실행한다. 테스트에 selector를 재구현하지 않는다.
  우선 외부 Pester 설치 없이 명시적 실패 exit code와 case 결과를 반환한다.
- **작은 Windows job**: 기존 Desktop dispatcher를 통해 짧은 reusable workflow를 호출한다.
  계약 테스트 후 직접 만든 공개 fixture용 저장창에서 소유 관계·지원 pattern·확인창
  형태를 관측한다. 자동 Yes를 보내지 않고 닫기/실패 cleanup까지 제한한다.
  앱 설치·Rust/driver build·submodule·문서 렌더링·서명은 하지 않는다.
- **adapter 확정**: 수집한 관측과 API 계약에 따라 지원 경로 하나를 선정한다. UIA pattern,
  native button, task-dialog 전용 메시지를 근거 없이 차례로 시도하지 않는다.
  `#32770`/Yes-No 모양만으로 task dialog라고 단정하지 않는다.
- **기록**: 안전한 fixture의 관측만 축약 보존한다. 개인 경로·본문·stack trace는 기록하지
  않으며 prompt는 예상 target 일치 판정과 필요한 정규화 결과로 남긴다.

### 제외

제품 소스·rhwp·서명·릴리즈·updater·수동 수용 반복, 범용 UI 자동화 프레임워크,
Mac용 native 검증, self-hosted runner 설치, 전역 키/좌표 입력, 무조건 Yes,
기존 전체 PDF workflow의 timeout 증가와 변경 없는 반복 실행은 제외한다.

### 잠정 단계와 검증

1. **Stage 4.17 — 판단 분리·동작 계약·작은 관측 job**
   - 기존 helper가 순수 함수를 사용하도록 연결하고 중복 ID, 잘못된 process/parent,
     disabled/누락/중복 후보, unknown modal·wrong target·취소 조건을 실행 테스트한다.
   - Windows PowerShell 5.1에서 실제 함수 테스트 및 Win32 helper 로드/컴파일을 검사한다.
     합성 판단 입력의 통과와 실제 Windows OS 호출 검증을 구분한다.
   - 작은 dialog fixture에서 실제 소유 관계·capability·정규화된 확인창 정보를 수집한다.
     예측과 다른 경우 원인·미확정 항목을 보고하고 전체 PDF 실행으로 넘어가지 않는다.
2. **Stage 4.18 — 관측된 overwrite adapter·작은 통합 검증**
   - 4.17 관측 검토·단계 승인 뒤 확인된 API만 구현한다. 같은 실제 helper/adapter를
     fixture에서도 사용하고 열기·새 저장·overwrite·취소/거부의 사후 조건을 확인한다.
     fixture-host 성공을 Alhangeul 설치본 수용이라고 부르지 않는다.
3. **Stage 4.19 — 기존 제품 설치본 전체 검증**
   - 위 경계가 통과하고 승인된 뒤 기존 SHA/native artifact의 유효성을 확인한다.
     Alhangeul HWP/HWPX fresh/restart와 PDF 분석을 한 번 실행한다.
     제품 bytes가 바뀌지 않았다면 이 자동화 변경 때문에 재빌드하지 않는다.

각 단계 종료 때 승인받으며, 4.17의 첫 원격 실행 전 계획된 검사 전체를 검토한다.
작은 job은 `windows-2025`, 읽기 전용 권한, 자동 retry 없음, 실패 증거 업로드와
자신이 만든 fixture process·정확한 임시 경로만 cleanup하는 경계로 둔다.
로컬 검증은 `pnpm run test:gui:windows:contracts`, `pnpm run typecheck:gui`,
변경 workflow 구문·권한·mode 분리 검사와 `git diff --check`로 제한한다.
PowerShell 동작 테스트는 Windows 실행 결과가 없으면 미검증으로 표시한다.

### 예상 파일과 문서 위치

- 신규 후보: `scripts/windows-pdf-dialog-policy.ps1`,
  `tests/windows-pdf-dialog-policy.test.ps1`, fixture host/probe script,
  축약 native-dialog fixture, `.github/workflows/alhangeul-windows-dialog.yml`.
- 수정: 기존 dialog/Win32 helper, Desktop dispatcher, Windows PDF 계약 테스트와
  `package.json` 실행 진입점. 새 의존성을 설치하지 않는 범위를 우선한다.
- 공식 기준은 기존 승인된 `docs/operations/NATIVE_UI_TESTING.md`의 현재 지원/공백만
  갱신한다. 독자는 자동화 작성자이며, 대안인 별도 공식 가이드는 만들지 않는다.
- 사건 근거는 기존 `mydocs/troubleshootings/task_m010_19_windows_pdf_automation.md`,
  계획·단계 보고·오늘할일은 기존 `mydocs/`에 둔다. 선택 이유는 Stage 4.16과 동일하다.
- 신규 파일 300 LOC/함수 50 LOC 이내로 역할을 나눈다. 초과나 의존성 추가가 필요하면
  변경 전에 계획을 보정한다.

### 위험과 승인 요청

fixture와 Alhangeul이 노출하는 dialog가 다를 수 있다. 작은 관측은 후보 API를 검증하는
근거이며 설치본 최종 검증을 대체하지 않는다. 미지원 언어·OS 변형은 명시적으로 거부한다.
테스트 실행기의 새 실패를 피하기 위해 외부 모듈 설치를 우선 추가하지 않는다.

위 구조와 작은 Windows job 도입을 승인받은 뒤 구현계획서의 단계별 파일·명령을 정렬하고
4.17을 구현한다. 4.18의 실제 overwrite 호출 방식은 관측 결과와 함께 별도 승인받는다.

## 2026-09-07 재발 방지 가이드·회귀 테스트 보강 승인

작업지시자가 이 대화에서 공통 가이드, 사건 기록, 실행 가능한 회귀 테스트의 연결을
제안받고 진행을 승인했다. 기존 #19의 Windows PDF 수용 자동화에 대한 보강이며 새
제품 기능이나 별도 자동화 프레임워크를 도입하지 않는다. 아래 문서 위치 판단은 이전의
"새 공식 문서는 만들지 않는다"는 재개 제한을 이번 승인 범위에 한해 대체한다.

| 산출물 | 독자·공식화 수준 | 선택 위치 / 대안 | 이유 |
|---|---|---|---|
| native UI 테스트 작성·진단 가이드 | 기여자·유지보수자, 공식 운영 기준 | `docs/operations/NATIVE_UI_TESTING.md` / `mydocs/manual/` | 이미 채택한 공식 운영 루트를 사용하며 Hyper-Waterfall 자체 매뉴얼과 분리한다. |
| 개발·문서 인덱스 및 릴리즈 체크리스트 링크 | 개발·릴리즈 작업자, 공식 진입점 | 기존 `docs/DEVELOPMENT.md`, `docs/README.md`, `docs/operations/RELEASE_CHECKLIST.md` / 별도 README | 규칙을 복제하지 않고 가이드로 연결한다. 릴리즈 gate는 변경하지 않는다. |
| 이번 실패와 미해결 사항 | 내부 작업자, 사건 근거 | `mydocs/troubleshootings/task_m010_19_windows_pdf_automation.md` / 단계 보고서 누적 | 알려진 실패를 한곳에서 찾고 관측 사실과 미검증을 구분한다. |
| 축약 재현 데이터·동작 테스트 | 자동화 작성자, 실행 가능한 회귀 | `tests/fixtures/windows-pdf-regressions.json`, `tests/windows-pdf-regressions.test.mjs` / raw Actions artifact | 개인 경로·본문 없이 실제 판정 함수에 실패 입력을 재생하고 원본 run provenance를 남긴다. |

이번 단계는 문서·기존 순수 함수의 회귀 테스트·pnpm 실행 진입점만 변경한다.
버튼 selector/Win32 helper의 구조 변경과 미지원 overwrite adapter 구현은 제외한다.
문자열 계약을 실제 버튼 동작 테스트라고 부르지 않는다. 원격 workflow, native build,
릴리즈·서명·게시를 실행하지 않고 focused tests·문서 링크·diff를 확인한다.
후속 실제 버튼 판단 함수 분리/Windows 대화상자 최소 검증은 다음 단계 승인 대상이다.

## 2026-09-06 재개 계획 — 승인 대기

아래 재개 기준은 이전 단계의 역사적 전제보다 우선한다. 기존 Stage 1~4.3 구현과
보고서는 보존하고 처음부터 다시 구현하지 않는다. 작업지시자는 #9 병합 뒤 #19 재개를
지시했으며, 이번 단계에서는 재개 계획과 오늘할일만 갱신한다.

- 기존 작업/원격 후보: `16149e8a99c7c870cca71d89bdc94f1e8069dc58`
- 통합 대상 devel: `c93ac8c58a796a45227f764f37b7aaffaa81899e` (PR #56 병합)
- #19는 M010 OPEN이며, 첫 공개 전 필수 수정이다. #9 준비 완료는 #19 수용을 대체하지 않는다.
- 기존 immutable snapshot·native TTL/reaper·startup cleanup 구현은 재사용 후보다.
  이전 native 성공은 당시 SHA의 증거이며 최신 통합 후보의 성공으로 간주하지 않는다.
- 최신 제품 기준인 rhwp v0.8.4 pin, 암호·source save 보호, updater·썸네일·Pages와
  릴리즈 계약을 유지한다. upstream 갱신이나 릴리즈 실행은 이번 범위가 아니다.

### 통합 방식과 예상 충돌

기존 `local/task19`에 최신 `devel`을 merge하여 8개 고유 커밋을 보존한다.
rebase, force push, 과거 구현의 일괄 덮어쓰기는 하지 않는다. 실제 merge는 승인 후 수행한다.
`git merge-tree` 모의 병합에서 다음 8개 충돌 파일을 확인했다.

- `.github/workflows/alhangeul-desktop.yml`
- `apps/desktop/src-tauri/src/lib.rs`
- `apps/studio-host/src/core/desktop-host.test.ts`
- `apps/studio-host/src/core/desktop-persistence.test.ts`
- `apps/studio-host/src/core/desktop-persistence.ts`
- `mydocs/orders/20260824.md`
- `tests/actions-workflows.test.mjs`
- `tests/rhwp-baseline.test.mjs`

workflow는 최신 게시·서명 경계를 유지하면서 #19 native gate의 중복 여부를 확인한다.
Studio persistence는 최신 source save/암호 보호와 snapshot export를 함께 보존한다.
native 초기화에는 기존 updater·preview 초기화를 보존하고 PDF reaper만 통합한다.
baseline 검사는 최신 pin 기반 검사를 유지하며 #19 PDF 계약만 결합한다.
자동 병합 파일도 commands·host·state의 의미 정합성을 검토한다.
보드는 양쪽 이력을 보존한다. 다른 task 브랜치와 리뷰 worktree는 수정하지 않는다.

### 남은 단계와 최소 검증

1. 재개 계획 승인 후 기존 구현계획서의 Stage 4 통합·수용 범위를 보정한다.
2. 승인된 통합 단계에서 merge 충돌을 해소하고 snapshot·persistence·native cleanup의
   현재 API 정합성을 확인한다. 기존 단계 구현을 반복하지 않고 필요한 보정만 수행한다.
3. 로컬에서는 PDF focused test 후 `check:product-boundary`, `test:upstream`,
   `test:studio`, `build:studio`와 변경 workflow 계약·구문 검사를 실행한다.
   실패 원인을 묶어 해결한 뒤 최종 candidate를 확정하며 문서만 바뀐 경우 native build를 반복하지 않는다.
4. 승인된 candidate exact SHA에서 Windows/Linux x64 Rust test·Clippy·앱 build와 PDF 수용을
   수행한다. 기존 workflow의 중복 gate를 먼저 확인하고 같은 SHA의 결과를 재사용한다.
   Linux GUI는 해당 후보 artifact로 확인하고, Windows 실제 PDF·reload/회수 수용은 별도
   증거로 기록한다. 실행 환경이 없으면 미검증을 명시하고 완료 처리하지 않는다.
5. 혼합 revision 방지, source dirty/암호·save 보호, 같은 target 경합과 atomic replace,
   TTL·reload·실패·startup cleanup 수용이 충족된 뒤 최종 보고·devel PR로 인계한다.

제품 설계나 검증 범위가 커지면 자동 확대하지 않고 재승인을 요청한다.
Mac에서는 플랫폼 중립 검증만 수행한다. 릴리즈·tag·서명·게시·updater 활성화는 수행하지 않는다.

### 문서 위치 및 승인 요청

기존 문서 위치 판단을 유지한다. 이번 재개 기록은 본 수행계획서와
`mydocs/orders/20260906.md`에만 둔다. 후속 구현계획·단계 보고는 기존 `mydocs/`에,
공식 PDF 계약 변경은 이미 승인 대상으로 정한 `docs/architecture/UPSTREAM.md`와
`docs/operations/DESKTOP_RELEASE.md`에 최소 반영한다. 새 공식 문서는 만들지 않는다.

승인 요청: 기존 이력을 보존하는 devel merge, 위 8개 충돌의 의미 단위 보정,
최신 제품 보호 경계 유지 및 최종 후보 중심의 Windows/Linux 최소 수용 순서.
승인 전에는 실제 merge·제품 코드 변경·원격 CI 실행을 하지 않는다.

## 목적

직접 PDF 내보내기가 시작 시점의 한 문서 상태만 사용하도록 고정해, 페이지 SVG를 순차 수집하는 동안 편집이 발생하더라도 서로 다른 문서 revision이 한 PDF에 섞이지 않게 한다.

WebView reload, 창 종료, timeout과 변환 실패로 정상 완료되지 못한 native PDF job과 OS 임시 SVG를 회수한다. 기존의 같은 target 잠금, searchable PDF와 outlined fallback, atomic target replace, source dirty/session/recent 불변 계약은 유지한다.

## 배경

현재 `DesktopPersistence.exportPdf()`는 active Studio handler에서 `pageCount()`를 얻은 뒤 각 `getPageSvg(page)`를 `await`하며 native job에 전달한다. 각 페이지 호출 사이에 편집이 들어오면 앞뒤 페이지가 다른 문서 상태에서 렌더될 수 있지만, native job은 owner·target·페이지 순서만 확인하므로 이를 식별하지 못한다.

native `PdfExportJobs`는 `TempDir`로 정상 abort·commit·window destroy 시 임시 파일을 회수하고 같은 target의 동시 작업을 막는다. 그러나 WebView reload는 native window를 파괴하지 않으므로 job record와 target 잠금이 프로세스 종료까지 남을 수 있고, 비정상 종료 뒤 OS temp의 `.alhangeul-pdf-*` 디렉터리를 다음 실행에서 정리하는 경계도 없다.

Task #19는 Issue #34의 Linux GUI 자동 수용과 병렬로 시작하며, Task #20의 dispatcher·embed handler lifecycle 정리도 별도 worktree에서 진행 중이다. 따라서 이번 task는 `command/dispatcher.ts`, `embed/desktop-runtime.ts`, `core/platform.ts`와 native `desktop_platform`을 수정하지 않는 경계를 우선한다. 해당 파일이 필요해지면 Task #20과의 통합 순서를 확인하고 수행계획 보정 승인을 받는다.

## 범위

### 포함

- 현재 문서 형식(HWP/HWPX)의 serializer bytes를 한 번 캡처하고 격리된 WASM 문서 인스턴스로 재로딩해 page count와 모든 SVG를 같은 immutable snapshot에서 생성하는 Studio 경계
- snapshot 생성·페이지 렌더·해제 lifecycle과 HWP/HWPX 대표 fixture의 원본 대비 쪽 수·본문·페이지 SVG 정합성 검증
- snapshot 식별자와 page count를 native PDF job에 결속하고 다른 snapshot, 중복·누락·역순 페이지를 fail-closed 처리하는 command 계약
- page count, 단일 SVG bytes, 누적 SVG bytes, job 수명과 동시 job 수에 대한 bounded limit
- 새 export 시작 시 같은 window owner의 중단 job 회수, window destroy 회수, TTL 기반 stale job 회수
- 애플리케이션 시작 시 충분히 오래된 `.alhangeul-pdf-*` orphan temp 디렉터리를 prefix·age·directory 검증 뒤 제한된 개수만 정리하는 경계
- 편집 중 export, WebView reload, timeout, append/변환 실패, 같은 target 경합과 비정상 종료 흔적의 focused test
- Windows x64와 Linux x64 exact-SHA에서 직접 PDF의 쪽 수, searchable 한글 text, source state 불변과 임시 파일 회수 검증
- 기존 공식 아키텍처·release 운영 문서의 PDF snapshot 및 잔여 수동 gate 최소 정렬

### 제외

- `svg2pdf`, `pdf-writer`, bundled font와 outlined fallback 정책 변경
- 시스템 인쇄 `file:print` 또는 Issue #15의 print surface 변경
- HWP/HWPX source save UX, serializer 기능 또는 암호 정책 변경
- upstream `third_party/rhwp` 직접 수정과 Task #20의 adapter lifecycle·dead bridge 정리
- autosave/native recovery 저장소, release tag·GitHub Release·서명·게시·updater 활성화
- macOS build·검증·배포

## 설계 방향

- PDF page surface의 진실 원천은 live editor가 아니라 export 시작 시 한 번 만든 immutable snapshot으로 둔다. active document를 현재 형식의 serializer bytes로 캡처하고 별도 WASM document에 로드한 뒤, 그 인스턴스에서 page count와 모든 page SVG를 생성한다.
- snapshot 캡처는 source save나 `notifySaved`를 호출하지 않고 active document의 file path, format, native revision, dirty, recent와 upstream recovery 상태를 바꾸지 않는다. snapshot 인스턴스는 성공·실패·취소 모두에서 명시적으로 해제한다.
- HWP/HWPX serializer round-trip 뒤의 page count와 대표 SVG 구조가 live document와 동등한지 Stage 1 fixture로 먼저 검증한다. 조판 손실 또는 허용할 수 없는 메모리 증가가 확인되면 mutation lock이나 revision guard로 임의 전환하지 않고 대안과 영향 범위를 문서화해 수행계획 보정 승인을 요청한다.
- native job은 `snapshotId`, 예상 page count, owner window, target path, 생성·최근 활동 시각과 누적 SVG bytes를 소유한다. 모든 append·commit에서 owner/snapshot/page order/limit/expiry를 함께 검증한다.
- 새 begin은 먼저 expired job을 회수하고 같은 window owner의 이전 미완료 job을 정리한다. target lock은 다른 window의 유효 job에 대해서만 유지한다. window destroy cleanup은 기존 경계를 보존한다.
- orphan temp cleanup은 OS temp 루트 바로 아래의 제품 prefix directory만 대상으로 하고, symlink·최근 항목·한도 초과 항목은 건드리지 않는다. 사용자 문서 경로나 target PDF를 재귀 정리 대상으로 사용하지 않는다.
- limit과 TTL의 구체값은 대표 6쪽 fixture와 긴 문서 focused test에서 page/SVG 크기와 실행 시간을 측정한 뒤 구현계획서에 고정한다. limit 초과는 target을 바꾸지 않은 채 명시적 오류와 cleanup으로 끝낸다.
- Task #20과 충돌을 피하기 위해 우선 예상 변경을 새 snapshot module, `DesktopPersistence`/`DesktopHost`, native PDF job·command에 한정한다. Task #20 또는 #34가 먼저 merge되면 승인 후 최신 `devel`을 통합하고 focused·전체 gate를 다시 실행한다.

## 문서 위치 판단

| 파일 | 분류 | 대상 독자 | 선택 위치 | 대안 위치 | 선택 이유 |
|---|---|---|---|---|---|
| `docs/architecture/UPSTREAM.md` | 공식 아키텍처 문서 | 기여자·유지보수자 | `docs/architecture/UPSTREAM.md` | `mydocs/tech/` | 현재 문서가 live `getPageSvg`와 native PDF job 소유 경계의 진실 원천이므로 snapshot 경계를 같은 위치에서 최소 보정한다. |
| `docs/operations/DESKTOP_RELEASE.md` | 공식 운영 문서 | 릴리스 작업자 | `docs/operations/DESKTOP_RELEASE.md` | `mydocs/working/` | Windows/Linux exact-SHA PDF 수용과 임시 artifact 정리 기준을 이미 소유하므로 mutation·reload·stale cleanup gate를 추가한다. |
| `mydocs/plans/task_m010_19*.md`, `mydocs/working/task_m010_19_stage*.md`, `mydocs/report/task_m010_19_report.md` | 작업 산출물 | 작업지시자·내부 작업자 | `mydocs/` | `docs/` | 승인·단계 검증·최종 판단은 제품 공식 문서와 분리한다. |

## 예상 변경 파일

신규:

- `apps/studio-host/src/core/pdf-export-snapshot.ts`
- `apps/studio-host/src/core/pdf-export-snapshot.test.ts`

수정:

- `apps/studio-host/src/core/desktop-persistence.ts`
- `apps/studio-host/src/core/desktop-persistence.test.ts`
- `apps/studio-host/src/core/desktop-host.ts`
- `apps/studio-host/src/core/desktop-host.test.ts`
- `apps/desktop/src-tauri/src/pdf_jobs.rs`
- `apps/desktop/src-tauri/src/commands.rs`
- `apps/desktop/src-tauri/src/lib.rs`
- `docs/architecture/UPSTREAM.md`
- `docs/operations/DESKTOP_RELEASE.md`

이번 task 산출물:

- `mydocs/orders/20260824.md`
- `mydocs/plans/task_m010_19.md`
- `mydocs/plans/task_m010_19_impl.md`
- `mydocs/working/task_m010_19_stage{N}.md`
- `mydocs/report/task_m010_19_report.md`

예상 목록 밖의 upstream source, dispatcher/embed runtime, workflow 또는 새 공식 문서 변경이 필요하면 해당 단계에서 멈추고 계획 보정 승인을 요청한다.

## 잠정 단계

- **Stage 1 — immutable PDF snapshot 계약**
  - 현재 형식 serializer bytes로 격리 snapshot을 만들고 같은 snapshot의 page count·SVG만 반환하는 lifecycle을 구현한다.
  - HWP/HWPX round-trip 조판 정합성, export 중 live 편집과 source dirty/session 불변, snapshot 해제를 focused test로 검증한다.
- **Stage 2 — native job freshness와 resource limit**
  - snapshot 결속, owner/target/page order 검증, TTL·same-owner recovery, page/SVG/job limit과 orphan temp cleanup을 구현한다.
  - 만료·reload·실패·경합에서 target 보존과 temp/job 회수를 Rust test로 검증한다.
- **Stage 3 — Studio/native 통합과 공식 문서 정렬**
  - snapshot pipeline을 direct PDF command에 연결하고 cancel·timeout·fallback·동시 요청 회귀를 검증한다.
  - 공식 아키텍처와 release 운영 문서에 자동 gate와 잔여 수동 gate를 최소 반영한다.
- **Stage 4 — Windows/Linux exact-SHA 수용**
  - 최신 승인 `devel`을 통합한 exact SHA에서 플랫폼 중립 gate와 Windows/Linux native build·PDF 시나리오를 실행한다.
  - 편집 중 export 결과 일관성, searchable 한글 text, atomic replace, reload/재실행 cleanup과 검증 한계를 기록한다.

## 검증 계획

### 단계별 검증

- Stage 1
  - `pnpm run test:studio`
  - `pnpm run build:studio`
  - HWP/HWPX fixture의 live/snapshot page count와 대표 page SVG 구조 비교
- Stage 2
  - Windows/Linux native 환경의 `pnpm run test:desktop`
  - Windows/Linux native 환경의 `pnpm run clippy:desktop`
  - expired/same-owner/cross-window/target-lock/page·bytes limit/orphan cleanup focused Rust test
- Stage 3
  - `pnpm run check:product-boundary`
  - `pnpm run test:automation`
  - `pnpm run test:upstream`
  - `pnpm run test:studio`
  - `pnpm run build:studio`
  - `git diff --check`
- Stage 4
  - Windows/Linux exact-SHA native workflow의 `test:desktop`, Clippy와 Tauri bundle build
  - Linux x64 GUI acceptance에서 직접 PDF evidence의 쪽 수·한글 text·render read-back
  - Windows x64에서 편집 중 export, reload 뒤 재시도, 같은 target 경합과 기존 target 보존 시나리오
  - OS temp의 제품 prefix orphan 및 native job record 회수 확인

### 통합 검증

- 한 PDF의 page count와 모든 page SVG가 하나의 immutable snapshot ID에 결속된다.
- export 중 live editor를 수정해도 결과가 시작 snapshot으로 완성되거나 명시적으로 실패하며 혼합 revision PDF는 target에 commit되지 않는다.
- WebView reload, window destroy, timeout, limit 초과와 변환 실패 뒤 job·temp가 회수되고 기존 target과 source session 상태가 보존된다.
- searchable PDF, outlined fallback 경고, 같은 target 원자 교체와 `notifySaved` 미호출 계약이 유지된다.
- `git status --short`가 PR 준비 전 빈 출력이다.
- `git diff --check`가 경고 없이 통과한다.

## 리스크

- **serializer snapshot 조판 차이**: 현재 형식 round-trip이 live IR의 일부를 손실하면 PDF가 시작 화면과 달라질 수 있다. Stage 1에서 HWP/HWPX 대표·긴 문서를 비교하고 차이가 있으면 구현을 확대하지 않고 대안 승인으로 되돌린다.
- **메모리·응답 시간 증가**: serializer bytes, 격리 document와 SVG가 일시적으로 공존한다. page/SVG/누적 bytes와 job 시간 limit을 두고 snapshot·native temp를 단계적으로 해제한다.
- **orphan cleanup 오삭제**: OS temp scan이 넓으면 다른 프로세스나 사용자 파일을 건드릴 수 있다. 고정 prefix, 바로 아래 directory, age, symlink 거부, 회수 개수 상한을 모두 만족한 항목만 삭제한다.
- **Task #20 충돌**: dispatcher/embed lifecycle branch가 병렬 진행 중이다. 해당 파일을 수정하지 않는 방향을 우선하고, 실제 필요 시 두 task의 diff와 merge 순서를 승인받는다.
- **Task #34 exact-SHA 변동**: Linux GUI acceptance 보정이 아직 진행 중이다. 과거 성공 artifact를 재사용하지 않고 #34 correction merge 이후 최신 기준으로 Stage 4를 실행한다.
- **지원 환경 제한**: 현재 macOS host에서는 Rust desktop/Tauri 성공을 주장하지 않는다. Rust·Tauri와 실제 PDF 수용은 Windows/Linux native 환경에서만 완료 판정한다.

## 승인 요청 사항

- live editor의 순차 `getPageSvg` 대신 현재 형식 serializer bytes로 만든 격리 WASM immutable snapshot을 직접 PDF의 진실 원천으로 사용하는 방향
- snapshot round-trip 정합성 또는 자원 한계가 실패하면 mutation lock/revision guard로 자동 전환하지 않고 수행계획을 보정하는 gate
- native job에 snapshot·owner·target·page/bytes/time limit을 결속하고 same-owner begin, TTL, window destroy와 app startup orphan cleanup을 함께 소유하게 하는 범위
- 공식 문서는 기존 `docs/architecture/UPSTREAM.md`와 `docs/operations/DESKTOP_RELEASE.md`만 최소 보정하는 위치 판단
- Task #20의 dispatcher/embed lifecycle 파일을 피하고, #34·#20 선행 merge가 생기면 최신 `devel` 통합 후 재검증하는 병렬 작업 경계
- macOS에서는 플랫폼 중립 검증만 수행하고 Rust desktop/Tauri·실제 PDF 수용은 Windows/Linux exact-SHA 환경에서 완료하는 검증 범위

승인되면 `task_m010_19_impl.md`에서 단계별 산출물, 검증 명령, limit/TTL 값, 커밋 메시지와 Task #20·#34 통합 순서를 구체화한다.
